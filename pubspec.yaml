name: planning_activites
description: Agendas .ics pour activités extrascolaires, hors vacances scolaires.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  archive: ^3.4.10
  file_saver: ^0.2.12
  file_picker: ^8.0.0
  shared_preferences: ^2.2.3
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/calendars/2026-2027.json
